<!DOCTYPE html>
<html lang="en">
<head>
    <style> 
        body {
            background-color: rgb(66, 49, 113);
            color: #ffffff;
            font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
            padding: 20px;
            margin: 0;
            text-align: center;
        }
        
        h1 {
            margin-bottom: 20px;
            color: #e0e0e0;
        }
        
        form {
            margin-bottom: 30px;
        }

        input[type="text"] {
            padding: 8px;
            width: 300px;
            margin-right: 10px;
            border: none;
            border-radius: 5px;
        }

        button {
            background-color: #6a5acd;
            color: white;
            padding: 8px 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }

        button:hover {
            background-color: #5a4cb1;
        }

        table {
            width: 80%;
            margin: 0 auto;
            border-collapse: collapse;
            background-color: #403270;
            color: #ffffff;
            border-radius: 8px;
            overflow: hidden;
        }

        th, td {
            padding: 12px;
            border: 1px solid #6a5acd;
            text-align: center;
        }

        th {
            background-color: #6a5acd;
        }

        tr:nth-child(even) {
            background-color: #504584;
        }

        p {
            margin-bottom: 20px;
        }

        a {
            color: #00ff7f;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
</head>
<body>
    <h1>URL Shortener</h1>

    <% if (id) { %>
        <p>Your short URL: <a href="http://localhost:8000/<%= id %>">http://localhost:8000/<%= id %></a></p>
    <% } %>

    <form method="POST" action="/url">
        <label for="url">Enter URL:</label>
        <input type="text" id="url" name="url" placeholder="Enter URL">
        <button>Generate</button>
    </form>

</body>
</html>
